# certwatch
**IMPORTANT**
```html
Before running watcher.sh you must run the initial subdomain scan e.g:

$ subfinder -dL /root/certwatch/domains-watcher.txt -all -o /root/certwatch/subfinder-watcher.txt

Otherwise we will receive Python subprocess errors in our email via crontab
```
